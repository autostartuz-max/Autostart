{
  "name": "yhq-test",
  "version": "0.1.0",
  "private": true,
  "description": "YHQ Test — Telegram Mini App (yo'l harakati qoidalari imtihoniga tayyorlov)",
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev": "concurrently -n api,web,admin -c green,cyan,magenta \"npm:dev:api\" \"npm:dev:web\" \"npm:dev:admin\"",
    "dev:api": "npm run dev -w apps/api",
    "dev:web": "npm run dev -w apps/web",
    "dev:admin": "npm run dev -w apps/admin",
    "build": "npm run build -w apps/web && npm run build -w apps/admin && npm run generate -w apps/api && npm run db:deploy -w apps/api",
    "start": "npm run start -w apps/api",
    "db:setup": "npm run db:setup -w apps/api",
    "seed": "npm run seed -w apps/api"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
